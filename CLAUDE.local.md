# CLAUDE.local.md

**NOTE**: This file is for personal, machine-specific overrides only.
Add this file to `.gitignore` — it should NEVER be committed.

## Package Management Override
- ALWAYS use `uv` for Python package management. 
- Use `uv venv` instead of `python3 -m venv` or `virtualenv`
- Use `uv pip install` instead of `pip install`

## Local Environment

# Local dev URL: http://localhost:3000
# Local API URL: http://localhost:8000
# Local docs: http://127.0.0.1:8080

## Personal Preferences

# Verbose output during development (set to false for production)
# Debug mode enabled for detailed error messages
